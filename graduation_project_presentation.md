# شرح مشروع التخرج: Donation Platform

تم إعداد هذا الشرح خصيصاً بناءً على هيكل وملفات الكود الموجودة في المشروع، ليكون مرجعاً لك في مناقشة مشروع التخرج.

---

## 1. فكرة المشروع العامة
* **ما اسم المشروع؟** Donation Platform (منصة التبرعات).
* **ما المشكلة التي يحلها؟** غياب الثقة بين المتبرعين والمستفيدين المجهولين، وصعوبة التحقق من صحة احتياج المستفيدين عبر الإنترنت.
* **من هم المستخدمون؟** 
  - متبرع (Donor)
  - مستفيد (Beneficiary)
  - جمعية خيرية (Charity)
  - مدير النظام (Admin)
* **ما الهدف الأساسي منه؟** توفير بيئة آمنة وشفافة للربط بين المتبرع والمحتاج، مع إضافة طبقة موثوقية تتمثل في الجمعيات الخيرية التي تقوم بالتحقق من المستفيدين، وتوفير أدوات تواصل مباشرة.

---

## 2. شرح الأدوار داخل النظام
تم تعريف الأدوار في قاعدة البيانات (في ملف `User.model.js` ضمن `role` و `user_type`):
* **المتبرع (Donor):** يتصفح طلبات التبرع، يقدم التبرعات العينية أو المالية، ويمكنه التواصل مع المستفيد.
* **المستفيد (Beneficiary):** شخص محتاج يقدم طلب تبرع، ولكن يجب أن يتم توثيقه وحالته تكون `verified` قبل الاعتماد.
* **الجمعية الخيرية (Charity):** دور محوري، حيث تمتلك لوحة تحكم تتيح لها توثيق المستفيدين (Beneficiary Verification).
* **الأدمن (Admin):** دور خاص بـ `role: admin`، يمتلك صلاحيات إدارة النظام، مراقبة المنصة بالكامل عبر `AdminDashboard`.

---

## 3. طريقة عمل المشروع من البداية للنهاية
* **تسجيل الحساب والدخول:** التسجيل إما بالإيميل وكلمة المرور (Local)، أو عبر Google باستخدام Firebase. عند تسجيل الدخول، يستلم المستخدم Token يُحفظ في `localStorage`.
* **إضافة تبرع وعرضها:** يقوم المتبرع بإضافة تبرع من صفحة `AddDonation`، وتُعرض للمستخدمين في صفحة `Donations`.
* **طلب التبرع:** يقوم المستفيد بتقديم `DonationRequest` لطلب المساعدة.
* **التحقق (Verification):** يمر المستفيد بمرحلة توثيق من الجمعية من خلال `BeneficiaryVerificationModal` ليصبح `verified`.
* **الرسائل (الشات):** يتم التواصل الفوري بين المتبرع والمستفيد عبر نافذة `ChatDialog` باستخدام تقنية Socket.IO.
* **الإشعارات:** توجد تنبيهات للمستخدمين عند تلقي رسائل أو تحديثات على حالة التبرع عبر مكونات مثل `MessageBadge` و `DemoNotifications`.
* **لوحات التحكم:** 
  - `AdminDashboard`: للأدمن فقط لإدارة الموقع.
  - `CharityDashboard`: للجمعية الخيرية لإدارة التوثيقات.
  - `Dashboard` / `UserDashboard`: للمستخدم العادي للوصول لبياناته.

---

## 4. شرح تقني للمشروع
* **الواجهة (Frontend):** مبنية بـ **React 19** و **Vite** لسرعة البناء، و **Tailwind CSS v4** للتصميم السريع والممتاز، مع **Zustand** لإدارة الحالة و **Context API**.
* **الخلفية (Backend):** مبنية بـ **Node.js** و إطار عمل **Express**.
* **قاعدة البيانات:** **MongoDB**، وتم استخدام مكتبة **Mongoose** لبناء الـ Models.
* **الربط بين Frontend و Backend:** يتم عن طريق إرسال طلبات من خلال مكتبة **Axios**، ويوجد (Interceptor) يقوم بإضافة `Bearer Token` تلقائياً لأي طلب يحتاج صلاحية.
* **نظام التوثيق (Authentication):** نظام مزدوج، يتم استخدام **JWT (JSON Web Token)** للدخول العادي، و **Firebase** للدخول عبر جوجل.
* **الاتصال المباشر (اللحظي):** استخدام **Socket.IO** لدعم الشات المباشر (Real-time).

---

## 5. شرح هيكل الملفات
* **الفولدر `src` (في الواجهة):**
  * `pages`: يحتوي على الصفحات مثل `Home`, `Donations`, `AdminDashboard`.
  * `components`: المكونات القابلة لإعادة الاستخدام مثل `Navbar`, `Footer`, `ChatDialog`.
  * `context`: لإدارة البيانات العامة مثل `AuthContext` (لتسجيل الدخول) و `LanguageContext` (لدعم اللغتين العربية والإنجليزية).
  * `services` و `utils`: دوال مساعدة لضبط الـ API والـ Axios.
* **الفولدر `backend` (في الخلفية):**
  * `routes`: مسارات الـ API (مثل `auth.routes.js` للتوثيق، `donation.routes.js` للتبرعات).
  * `models`: هيكل الجداول في قاعدة البيانات (`User.model.js`, `Donation.model.js`).

---

## 6. شرح أهم الصفحات
* **`Home.tsx`:** الصفحة الرئيسية تعرض نبذة عن المنصة وإحصائيات عامة (متاحة للكل).
* **`AdminDashboard.tsx`:** صفحة محمية لا يدخلها إلا الأدمن للتحكم بالمستخدمين والمنصة. مرتبطة بـ `ProtectedRoute`.
* **`CharityDashboard.tsx`:** لوحة مخصصة للجمعيات الخيرية للتحقق من بيانات المستفيدين.
* **`Donations.tsx` و `AddDonation.tsx`:** لعرض التبرعات المتاحة وإضافة تبرعات جديدة، تحتوي على مكونات الـ UI لرفع الصور وإضافة التفاصيل.
* **`BeneficiaryVerification.tsx`:** صفحة لرفع وثائق المستفيد وطلب التوثيق.

---

## 7. شرح أهم المكونات Components
* **`Navbar` / `Footer`:** القوائم العلوية والسفلية الموجودة في كل الصفحات للتنقل.
* **`ProtectedRoute`:** مكون هام جداً من الناحية الأمنية، يغلف الصفحات الحساسة ولا يسمح بعرضها إلا إذا كان المستخدم يملك `Token` صالح والصلاحية المناسبة (`allowedRole`).
* **`ChatDialog` و `MessageBadge`:** نوافذ المحادثات التي تستقبل البيانات عبر `Socket.IO` وتعرض إشعارات بالرسائل الجديدة.

---

## 8. شرح Backend
* **الـ Routes:** مسؤولة عن استقبال الطلبات. مثلاً: `/api/auth` (للتسجيل والدخول)، `/api/donations` (لإنشاء أو جلب التبرعات).
* **الـ Models:** الجداول التي تحدد شكل البيانات (`User`, `Donation`, `Message`).
* **الـ Controllers:** المنطق البرمجي، غالباً مدمج مع مسارات الـ Routes، يقوم باستقبال الطلب من الواجهة وتنفيذ عملية الحفظ (`save`) في قاعدة البيانات.
* **التحقق من المستخدم:** يرسل المستخدم الإيميل والباسورد للـ Backend، يتم التأكد منهما وإرجاع `JWT`، يُخزن في المتصفح ويرسل مع الطلبات اللاحقة ليتأكد الـ Backend من هوية المرسل.

---

## 9. شرح قاعدة البيانات (MongoDB)
قاعدة البيانات NoSQL (تخزن البيانات كـ Documents)، أهم الجداول (Collections):
* **`Users`:** يضم بيانات المسجلين (الاسم، الإيميل، نوع المستخدم، حالة التوثيق).
* **`Donations` و `DonationRequests`:** يضم تفاصيل عروض وطلبات التبرع.
* **`Conversations` و `Messages`:** لتخزين المحادثات بين الأطراف لضمان بقاء السجل التاريخي للشات.
* **`Campaigns` و `VolunteerOpportunities`:** للفرص التطوعية والحملات الكبيرة.

---

## 10. سيناريو استخدام كامل
1. يسجل مستخدم حساباً جديداً بنوع مستفيد (`beneficiary`).
2. لا يمكنه طلب التبرع فوراً؛ يقوم برفع أوراقه الثبوتية.
3. تدخل الجمعية الخيرية عبر حساب `charity` وتقوم بمراجعة بياناته وتوثيق حسابه (`verifiedBy: 'charity'`).
4. بعد التوثيق، ينشئ المستفيد طلب تبرع جديد (`DonationRequest`).
5. يدخل متبرع (`donor`) للموقع، يتصفح صفحة `Donations` ويجد الطلب.
6. يضغط المتبرع على زر التواصل، فتفتح له نافذة الشات (`ChatDialog`) ويبدأ المحادثة المباشرة مع المستفيد للاتفاق.

---

## 11. نقاط القوة في المشروع
* **تقنيات حديثة:** استخدام React 19 و Tailwind CSS v4، وهي أحدث ما في السوق، مما يوفر أداء رائعاً.
* **الشات الفوري (Real-time):** إضافة Socket.IO يعطي تجربة مستخدم حية ممتازة وتواصل أسهل.
* **الثقة والأمان:** ميزة توثيق الجمعيات للمستفيدين تحل أكبر عائق في منصات التبرعات وهو الشفافية.
* **دعم لغات متعددة:** وجود `LanguageContext` لدعم النظام باللغتين العربية والإنجليزية.

---

## 12. نقاط يمكن تحسينها (للمناقشة)
* **نظام الدفع:** حالياً التبرع يعتمد على التوصيل أو الاتفاق الفردي، خطوتنا القادمة هي دمج بوابة دفع عالمية (مثل Stripe) للتبرع المالي الفوري.
* **تشفير كلمات المرور:** الدالة الحالية `comparePassword` في `User.model` تقارن النصوص العادية (Plain Text)، التحسين المستقبلي هو تفعيل مكتبة `bcrypt` الموجودة في المشروع لتشفير الباسورد بالكامل لزيادة أمان المستخدمين.

---

## 13. أسئلة متوقعة في المناقشة مع الإجابات
* **كيف ربطت بين الواجهة والخلفية (Frontend & Backend)؟**
  > استخدمنا مكتبة `Axios`. الـ Frontend يعمل على منفذ 5173 ويرسل الطلبات إلى `http://localhost:5000/api` الخاص بالـ Backend.
* **كيف قمتم بحماية الصفحات الخاصة مثل لوحة تحكم الأدمن؟**
  > بفضل مكون `ProtectedRoute` في الواجهة، ونظام `JWT Auth` في الخلفية. إذا لم يكن المستخدم يملك Token صالح و`role=admin`، سيتم طرده لصفحة الدخول.
* **كيف يعمل نظام المحادثات؟**
  > استخدمنا مكتبة `Socket.IO`. بمجرد تسجيل الدخول، يفتح اتصال دائم بين المستخدم والسيرفر، وعند إرسال رسالة يتم تمريرها فوراً للطرف الآخر بدون حاجة لتحديث الصفحة.
* **لماذا اخترتم MongoDB لقاعدة البيانات؟**
  > لأنها قاعدة بيانات مرنة (NoSQL) وتتوافق بشكل ممتاز مع تقنيات الجافاسكريبت و Node.js (استخدمنا Mongoose)، مما سهل علينا التعامل مع بيانات المستخدمين والمحادثات.

---

## 14. ملخص قصير للمناقشة (دقيقتين)
> "مشروعنا هو **Donation Platform**، وهي منصة ذكية وآمنة تهدف للربط بين المتبرعين والمحتاجين، تحت إشراف وتوثيق الجمعيات الخيرية. 
> المشروع يحل مشكلة كبيرة وهي انعدام الشفافية في التبرعات الفردية، لذلك جعلنا الجمعيات الخيرية طرفاً لتوثيق المستفيدين.
> المنصة مبنية بأحدث التقنيات: **React 19** في الواجهة بتصميم جذاب باستخدام **Tailwind v4**، و **Node.js** في الخلفية مع قاعدة بيانات **MongoDB**.
> من أهم الميزات التقنية في منصتنا هو نظام **المحادثات الفورية (Live Chat)** المعتمد على **Socket.IO** لتسهيل التواصل، ونظام حماية يعتمد على **JWT** لحماية بيانات المستخدمين، بالإضافة لكون المنصة تدعم اللغتين العربية والإنجليزية لتوسيع دائرة الاستفادة."
