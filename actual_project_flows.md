# آلية عمل المنصة الحالية (Actual Implemented Flows)

بناءً على الكود المكتوب والمنفذ فعلياً في المشروع حالياً، هذه هي الـ Flows الحقيقية بالتفصيل:

### 1. مسار تسجيل الدخول والتوثيق (Auth Flow) 🟢 شغال 100%
* **تسجيل عادي:** يعمل ويتصل بشكل مباشر بـ API (`/api/auth/register` و `/api/auth/login`).
* **تسجيل عبر جوجل (Firebase):** شغال، وفي حال كان المستخدم يدخل لأول مرة عبر جوجل، يظهر له `UserTypeModal` ليحدد نوعه (متبرع أم جمعية) قبل إكمال التسجيل.
* **الـ Token:** يُحفظ في `localStorage` وهناك Interceptor يضيفه تلقائياً لأي طلب يحتاج صلاحية.

### 2. مسار إضافة تبرع (Add Donation Flow) 🟢 شغال (مع خدعة بصرية)
* الفورمة حقيقية ومربوطة. عندما يضغط المتبرع "نشر التبرع"، يقوم الكود بإيقاف مؤقت لمدة 1.5 ثانية `setTimeout(..., 1500)` كحركة بصرية توحي بالمعالجة.
* بعد الثواني، يتم إرسال الطلب الحقيقي للـ Backend عبر `api.post('/donations')` ويُحفظ التبرع فعلياً في قاعدة البيانات.

### 3. مسار طلبات الجمعيات وعروض المتبرعين (Donation Requests & Offers) 🟢 شغال 100%
* المسار حقيقي بالكامل. ترفع الجمعية الخيرية الموثقة طلب احتياج (DonationRequest).
* يرى المتبرعون الطلب ويمكنهم تقديم عروض (DonorOffer).
* تقوم الجمعية بقبول العرض أو رفضه، وعند القبول تفتح محادثة لتنسيق الاستلام. عند استلام العنصر يتم رفع كمية الطلب المكتملة.

### 4. مسار المحادثات (Chat Flow) ⚠️ شغال (بواسطة Polling وليس Sockets)
* **`ChatDialog.tsx` (النافذة المنبثقة):** هذه النافذة **موهّمة (Mocked)** وتستخدم `localStorage` فقط ولا ترسل رسائل للـ Backend.
* **`ConversationChat.tsx` (صفحة الرسائل الحقيقية):** هذه هي الشاشة المربوطة فعلياً وتعمل بنجاح، لكن **تم إيقاف Socket.IO فيها**. تعتمد حالياً على **Polling**؛ أي أنها ترسل طلب API كل 5 ثواني `setInterval(fetchChat, 5000)` للتحقق من الرسائل الجديدة.
* **مسار القبول والاتفاق:** يبدأ الطلب كـ `pending`، وبعد ضغط الطرف الآخر على "قبول" تظهر أرقام الهواتف الخضراء للطرفين. والأزرار الخاصة بـ "تأكيد الاتفاق" و "تأكيد التسليم" مربوطة وتعمل في الـ Backend وتحدث قاعدة البيانات بنجاح.

### 5. مسار الإشعارات (Notifications Flow) 🟡 وهمي حالياً (Mock)
* نظام الإشعارات يعمل بناءً على سياق وهمي `NotificationContext` ومكونات Demo (`DemoNotifications`)، ولا يعتمد على اتصال حقيقي مع الـ Backend، باستثناء رسائل التنبيهات المنبثقة (Toasts) التي تظهر عند نجاح أو فشل الطلبات كاستجابة مباشرة من النظام.
